~/file/log/%Y/%m/%d: [cl, l]
~/file/log/%Y/%m: month

~/conf: [c, conf]
~/file: [f, file]
~/pkg: pkg

# These may be at different paths on different machines.
$JUMP_HOME/vault: a
$JUMP_HOME/git/kiln: kiln
$JUMP_HOME/git/kiln/journey: journey
$JUMP_HOME/git/kiln/brash: brash
$JUMP_HOME/git/md-tui: mdt
$JUMP_HOME/git/nushell/nushell: [nu, nushell]
$JUMP_HOME/git/wasm: wasm

# TODO: Does rust-kart belong in `~/pkg` or `$JUMP_HOME/git`?
#  - `../../src/install-dotfiles.zsh` clones it into `~/pkg`
#  - On my home machine, `~/pkg/rust-kart` is a symlink
$JUMP_HOME/git/rust-kart: rk
