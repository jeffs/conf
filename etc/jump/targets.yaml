~/file/log/%Y/%m/%d: [l, log]
~/file/log/%Y/%m: month

~/conf: [c, conf]
~/file: [f, file]
~/pkg: pkg
~/var/empty: [e, empty]

# These may be at different paths on different machines.
$JUMP_HOME/git/data: [d, data]
$JUMP_HOME/vault: a
