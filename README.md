# Environment configuration for macOS

This is my dotfiles repo for macOS. In theory, you should be able to reproduce my environment:

```sh
cd
git clone https://github.com/jeffs/conf
cd conf
src/init.zsh
cargo run --manifest-path prj/sync/Cargo.toml
````
